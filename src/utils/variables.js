const newOrder = "new";
const cancelledByClient = "cancelledByClient";
const accepted = "accepted";
const cancelledByBusiness = "cancelledByBusiness";
const done = "done";

const clientRole = "client";
const businessRole = "business";


module.exports = {
    newOrder, 
    cancelledByClient, 
    accepted, 
    cancelledByBusiness, 
    done,
    clientRole,
    businessRole
  };
  